This example is made only after creating a model using pytorch and exporting it using esp-ppq. 

This example will read the model input from an SD card. 

The Model input is stored in binary, one input per file; the directory is the label of the expected classification.